<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="@dimen/general_padding">

    <TextView
        android:id="@+id/item_response_user_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:lines="1"
        android:textColor="@color/black"
        android:textSize="@dimen/textSizeLarge"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="kennen" />

    <TextView
        android:id="@+id/item_response_content"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/general_padding"
        android:textColor="@color/primaryText"
        android:textSize="@dimen/textSizeNormal"
        app:layout_constraintEnd_toEndOf="@+id/item_response_user_name"
        app:layout_constraintStart_toStartOf="@+id/item_response_user_name"
        app:layout_constraintTop_toBottomOf="@+id/item_response_user_name"
        tools:text="货拉拉拉不拉拉布拉多" />

    <TextView
        android:id="@+id/item_response_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/general_padding"
        android:textColor="@color/primaryText"
        android:textSize="@dimen/textSizeNormal"
        app:layout_constraintStart_toStartOf="@+id/item_response_content"
        app:layout_constraintTop_toBottomOf="@+id/item_response_content"
        tools:text="2023-13-32" />

</androidx.constraintlayout.widget.ConstraintLayout>