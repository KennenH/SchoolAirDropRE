<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>

    <declare-styleable name="PageItem">
        <attr name="isFirst" format="boolean" />
        <attr name="isLast" format="boolean" />
        <attr name="isIconLarge" format="boolean" />
        <attr name="isSwitch" format="boolean" />
        <attr name="isCheck" format="boolean" />
        <attr name="showArrow" format="boolean" />
        <attr name="icon" format="reference" />
        <attr name="arrow" format="reference" />
        <attr name="title" format="string" />
        <attr name="description" format="string" />
    </declare-styleable>


    <!-- ConstraintContentContainer start-->
    <declare-styleable name="ConstraintContentContainer">
        <attr name="input_view" format="reference" />
        <attr name="hide_by_touch_area" format="reference" />
        <attr name="hide_by_touch_outside_enable" format="boolean" />
    </declare-styleable>

    <declare-styleable name="ConstraintPanelView">
        <attr name="constraint_panel_trigger" format="reference" />
        <attr name="constraint_panel_toggle" format="boolean" />
    </declare-styleable>
    <!-- ConstraintContentContainer end-->
</resources>
